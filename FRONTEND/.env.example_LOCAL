# Base API endpoints (production subdomain for local dev)
VITE_API_BASE=https://api.youreasysite.pl
VITE_API_BASE_URL=https://api.youreasysite.pl/api/v1

# Google OAuth (required for Studio builds)
VITE_GOOGLE_CLIENT_ID=

# Build configuration
#   STUDIO -> renders the SaaS application
#   SITE   -> renders a public site using VITE_SITE_ID
VITE_BUILD_TARGET=STUDIO
VITE_SITE_ID=
VITE_SITE_PAGE=home

# Tryb routingu dla stron publicznych: 'path' lub 'subdomain'
# 'path' -> localhost:3000/viewer/nazwa-strony
# 'subdomain' -> nazwa-strony.localhost:3000
VITE_APP_ROUTING_MODE=path