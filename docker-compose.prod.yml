# Production override for docker-compose
# Usage: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build

services:
  studio-frontend:
    build:
      target: production
    # Don't mount any volumes - remove ALL volume declarations
    volumes: !reset []
    environment:
      - NODE_ENV=production
