# Production override for docker-compose
# Usage: docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build

services:
  studio-frontend:
    build:
      target: production
    volumes: []  # Clear all volumes from base config
    environment:
      - NODE_ENV=production
